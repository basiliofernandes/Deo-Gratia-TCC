import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    txtG: {
        fontSize: 32,
        textAlign: "center",
        backgroundColor: "#007",
        justifyContent: "center",
        alignItems: "center",
        padding: 100
        
    }
})